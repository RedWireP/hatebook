<%= form_with(model: test) do |form| %>
  <% if test.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test.errors.count, "error") %> prohibited this test from being saved:</h2>

      <ul>
        <% test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :folder, style: "display: block" %>
    <%= form.text_field :folder %>
  </div>

  <div>
    <%= form.label :marka, style: "display: block" %>
    <%= form.text_field :marka %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
